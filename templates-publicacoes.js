/* =============================================================
   TRENDSCIENCE — Templates Publicações e Criativos
   Pool esvaziado em 07/09/2026 — sistema em reconstrução.
   Novos templates virão em próxima rodada.
   ============================================================= */

// Helpers reutilizáveis para os próximos templates
const WORDMARK_SVG = '<svg class="p-mark" viewBox="0 0 678 114" style="height:auto"><use href="#ts-wordmark"/></svg>';
const HANDLE_MARK  = '<svg class="handle-mark" viewBox="0 0 475 594"><use href="#ts-mono"/></svg>';
const HANDLE       = `<div class="handle">${HANDLE_MARK}<span>@trendscience</span></div>`;

window.__pcTemplates = [
  // Pool esvaziado em 07/09/2026 — aguardando novos templates.
];
